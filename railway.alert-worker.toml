[build]
builder = "DOCKERFILE"
dockerfilePath = "miniapp/Dockerfile"

[deploy]
preDeployCommand = "npm run db:migrate"
startCommand = "npm run worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
